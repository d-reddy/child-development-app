angular.module('MemoryService', []).factory('Memory', ['$http', function($http) {
        
        function Card() {
            var flipped = false;
            var srcImg = 0;
        };    

        var initialize = function (numberOfcards){
            
        }

}]);